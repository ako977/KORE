( function( $, ko, KORE, App ) {
	App.ViewModel.ApplicationViewModel = KORE.ViewModel.extend({
		init: function() {
			this._super();

			//=======================
			// App View Model
			// (Think of this as a stub for a viewmodel that represents your application, like a main( ) function)
			// You can initialize any other viewmodels you would use here
			//=======================

		}
	});
})( window.jQuery, window.ko, window.KORE, window.DEMO.app );